---
- name: Set core spcialised stack-specific variables
  set_fact:
    stack_name: "spcialised-stack"
    stack_config: "{{ spcialised_stack }}"
    stack_services:
      - plex
      - sonarr
      - radarr
      - prowlarr
    stack_compose_template: "{{ role_path }}/templates/docker-compose.yml.j2"

- name: Deploy spcialised stack using common docker-stack role
  include_role:
    name: base-stack
  vars:
    stack_name: "{{ stack_name }}"
    stack_config: "{{ stack_config }}"
    stack_services: "{{ stack_services }}"
    stack_compose_template: "{{ stack_compose_template }}
